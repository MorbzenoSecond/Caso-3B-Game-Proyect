extends Resource
class_name EventsScript

@export var Dialogues : Array[DialogueResource]
# Called when the node enters the scene tree for the first time.
func execute(body, markers_position_array):
	pass
